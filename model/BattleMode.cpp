#include "BattleMode.h"

void doBattle(IUnit players[], IUnit enemies[], int total_num_units) {
    bool battle_over = false;
    IUnit * turn_order = new IUnit[total_num_units];
    for (int i = 0; i < total_num_units; i ++) {

    }
    while (!battle_over) {

    }
}
