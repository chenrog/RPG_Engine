#include "BattleMode.h"

void doBattle(AUnit player, AUnit enemy, int total_num_units) {
    bool battle_over = false;
    AUnit * turn_order = new AUnit[total_num_units];
    for (int i = 0; i < total_num_units; i ++) {

    }
    while (!battle_over) {

    }
}
